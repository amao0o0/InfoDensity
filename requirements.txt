# Evaluation pipeline. The summaries in results/ were produced with vLLM 0.10.0;
# other versions are untested and greedy decoding is sensitive to the kernels
# vLLM selects (see results/README.md).
vllm==0.10.0
torch
transformers
peft            # merge_lora.py
datasets        # benchmark loading
pyarrow
sympy           # grader.py
pylatexenc      # math_normalize.py
numpy

# training/ additionally needs:
mathruler       # answer equivalence in the reward's correctness gate
